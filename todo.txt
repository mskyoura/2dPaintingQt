!!! обновлять дату релиза в wabout.ui !!!

2021-02-27
	можно выводить отладочную инфо (на индивидуальный и групповые ПБ):
	if (debugText != "") DrawText(i,0.7,  1.0, debugText,   Qt::gray, Qt::yellow);

	В режиме отладки почему-то ответы от платы не отрабатываются (нет ответа). В режиме релиза - работает, отлаживать через debugText.

2021-01-22
-. При установленной задержке отображать статус после этой задержки.
	
	2021-02-26
	- В режиме Debug при снятой блокировке Реле2 неверно отображается мигание красным (должен быть оранжевый без мигания)
	- искать изменения по 2021
	- под отладкой:
		- в индивидуальном - на команды ЗБ, РБ плата реагирует, но прога статус отображает мигающим; ПС - нормально;

	проверять в режиме сборки "Debug" - Тогда не нужно выбирать COM порт и будут автоматически отрабатываться команды

	нужно 5 ПБ, кликнуть на крайний левый, отобразится окно управления. Тогда видно крупный пульт и на фоне 5 остальных маленьких.
	Желтый статус должен отобразиться и на большом, и на малом пультах (они об одном и том же).



	getLastOperationWithGoodAnswer() - нужно сохранить предыдущий статус.

	




	См. pbsetup.cpp:709 730

	void Widget::updateTime()
	
pbsetup.cpp: if ((intT2 > 0) && (cmd == "ПК"))
vismo.cpp: 
	statusClr
	DrawText(i,0.7,  1.0, blnk,  statusClr,     Qt::white);
	

-. Изменить логотипы


2019-01-27
---------------------------------------------------------------------------------------
-. Не желтеет длинный статус в настройках.
void wAppsett::on_statLn0_textChanged(const QString &arg1)
cmd0 - команда
statLn0 - статус длинный
statSh0 - статус короткий

Vismo::fullStatusWidth = 0.7 * 3.0 * PBmodulesInnerWidth;
вызов: DrawText(i,0.7,  1.2, blnk, statusClr,     QColor(215, 215, 215), 1); //blnk - строка статуса
w = 648, maxTextWidth = 0.7 * 648 = 583

215*3

https://www.charlesodale.com/setting-qt-to-ignore-windows-dpi-text-size-personalization/

!!!!!!!! === передать Vismo::FitFontSize() в окно настроек? !!!!!!!!!!


textFont.setPixelSize(moTxtLineHeight*kFontSize); 

label_55 - выводить отладочную инфо

1) подкраска желтым

с командами хорошо - фонт и ширина подтягиваются из Vismo при непосредственном рисовании
            commandFont = textFont;
            commandWidth = maxTextWidth;

А для статуса приходится выдумывать.


2) уменьшать длинный статус, если вылезает за пределы

---------------------------------------------------------------------------------------
+. Изменить начальные надписи 
vismo.cpp: PBstatuses, PBcommands
pbsetup.cpp: Запустить Реле2, Реле1 включено - в предупреждении
wappset.ui: Отключать индикацию "Реле2 включено" в соответствии с настройками ПБ, Ограничить длительность Реле1 (1...255):

+. Срезает 9.66 (вольт) на большом ПБ.
Это везде, где слева - неЖирный, справа - Жирный
Vismo::DrawText() - есть место, где подкрашиваются тестовые прямоугольники для вывода текста.

+. Цвета в настройках названий съехали: нужно - Зел Крас Ор. Есть - Кр Зел Ор.